x, y, z = 0, 0, 0
print(f"{x} \n{y} \n{z}\n")

"""
A constant is a variable whose value stays the same
 throughout the life of a program. Python doesn’t have built
in constant types, but Python programmers use all capital
 letters to indicate a variable should be treated as a constant
 and never be changed
"""

MAX_CONNECTIONS = 5000
print(MAX_CONNECTIONS)